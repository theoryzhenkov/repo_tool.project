# Agent Instructions

## Project

TheoR Nebula project helper CLI package factories.

## Conventions

- Use `jj` for version control when available.
- Use `just` for task running.
- Run `nix flake check` before pushing changes.
